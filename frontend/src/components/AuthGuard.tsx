"use client";

// HACKATHON BUILD: AuthGuard disabled. All pages are open for judges to test.
export default function AuthGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
